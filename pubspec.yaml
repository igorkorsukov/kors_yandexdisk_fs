name: kors_yandexdisk_fs
description: A simple wrapper for the Yandex Disk API, making the interface similar to FS.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  http: ^1.1.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
